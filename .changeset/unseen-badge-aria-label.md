---
"@knocklabs/react-core": patch
"@knocklabs/react": patch
"@knocklabs/react-native": patch
---

Add accessible labels to `UnseenBadge` so screen readers announce the count with context (e.g. "3 unseen notifications") instead of just the bare number

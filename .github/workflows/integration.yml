name: Integration Tests

on:
  pull_request:
  workflow_dispatch:

jobs:
  run-integration-tests:
    name: React ${{ matrix.react-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - react-version: "16.14.0"
            testing-library-version: "12.1.5"
          - react-version: "17.0.2"
            testing-library-version: "12.1.5"
          - react-version: "18.2.0"
          - react-version: "19.1.0"
    env:
      INTEGRATION_KNOCK_PUBLIC_KEY: ${{ secrets.INTEGRATION_KNOCK_PUBLIC_KEY }}
      INTEGRATION_KNOCK_USER_ID: ${{ secrets.INTEGRATION_KNOCK_USER_ID }}
      INTEGRATION_KNOCK_FEED_ID: ${{ secrets.INTEGRATION_KNOCK_FEED_ID }}
    steps:
      - name: Checkout Latest
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: "package.json"
          cache: "yarn"
          registry-url: "https://registry.npmjs.org"
          scope: "@knocklabs"
      - name: Install Dependencies
        run: yarn install
        env:
          YARN_ENABLE_IMMUTABLE_INSTALLS: false
      - name: Build Packages
        run: yarn build:packages
      - name: Setup React Version
        run: yarn workspace @knocklabs/integration-web add react@${{ matrix.react-version }} react-dom@${{ matrix.react-version }}
        env:
          YARN_ENABLE_IMMUTABLE_INSTALLS: false
      - name: Setup Testing Library Version
        if: matrix.testing-library-version
        run: yarn workspace @knocklabs/integration-web add @testing-library/react@${{ matrix.testing-library-version }}
        env:
          YARN_ENABLE_IMMUTABLE_INSTALLS: false

      # - name: Setup React Version
      #   run: |
      #     jq --arg react_version "${{ matrix.react-version }}" \
      #       '.dependencies += {}
      #       | .dependencies["react"] = $react_version
      #       | .dependencies["react-dom"] = $react_version' package.json > tmp.json
      #     mv tmp.json ./integration/web/package.json
      # - name: Setup Testing Library Version
      #   if: matrix.testing-library-version
      #   run: |
      #     jq --arg testing_library_version "${{ matrix.testing-library-version }}" \
      #       '.devDependencies += {}
      #       | .devDependencies["@testing-library/react"] = $testing_library_version' package.json > tmp.json
      #     mv tmp.json ./integration/web/package.json
      # - name: Update Dependencies For Integration Tests
      #   run: yarn install
      #   env:
      #     YARN_ENABLE_IMMUTABLE_INSTALLS: false
      - name: Run Test
        run: yarn test:integration
